common:
  build_only: true
  tags:
    - drivers
    - wifi
tests:
  drivers.wifi.build_all.spi:
    extra_args: DTC_OVERLAY_FILE="spi_devices.overlay"
    extra_configs:
      - CONFIG_SPI=y
    platform_allow:
      - native_sim
      - native_sim/native/64
