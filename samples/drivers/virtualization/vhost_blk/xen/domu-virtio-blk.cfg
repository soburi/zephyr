# SPDX-License-Identifier: Apache-2.0
#
# Merge this entry into the existing DomU configuration. Keep it as the first
# and only virtio-mmio device while using the stock Xen 4.20 allocation layout.

virtio = [
    "type=virtio,device2,transport=mmio,backend=zephyr,grant_usage=1"
]
